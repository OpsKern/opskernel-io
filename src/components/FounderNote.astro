---

---

<section class="w-full px-6 md:px-12 lg:px-16 py-section">
  <div class="max-w-prose mx-auto">
    <div class="border-l-2 border-accent/40 pl-6 space-y-4 max-w-narrow">
      <p class="text-ink text-xl leading-relaxed">
        "I manage M365 environments for regulated organizations — email security, PowerShell,
        Copilot in VS Code every day. The scripts contain credentials, tenant configs, user data.
        Every policy doc says 'don't paste sensitive context into AI prompts.' That's a reminder. It
        relies on memory. I built hookd because memory is not enforcement."
      </p>
      <div class="flex items-center gap-3">
        <div
          class="h-8 w-8 rounded-full bg-surface-raised border border-ink-subtle/20 flex items-center justify-center font-mono text-xs text-ink-muted"
        >
          WS
        </div>
        <div>
          <p class="text-ink text-sm font-medium">Will Smith</p>
          <p class="text-ink-subtle text-xs font-mono">
            M365 &amp; email security admin · founder, OpsKern
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
